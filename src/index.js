/**
 * Webpack build entry point.
 *
 * Import block scripts, editor components, or other build-pipeline assets here.
 * Plugin static assets (banner.js, admin.js) are hand-authored and loaded
 * directly — they do not go through this pipeline.
 */
